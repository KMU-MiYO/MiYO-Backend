apiVersion: v1
kind: Service
metadata:
  name: miyo-backend-challenge-service
spec:
  selector:
    app: miyo-backend-challenge
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
