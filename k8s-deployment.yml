apiVersion: apps/v1
kind: Deployment
metadata:
  name: miyo-backend-posts-deployment
  labels:
    app: miyo-backend-posts
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: miyo-backend-posts
  template:
    metadata:
      labels:
        app: miyo-backend-posts
    spec:
      imagePullSecrets:
        - name: miyobackend
      containers:
      - name: miyo-backend-posts
        image: contest90.kr.ncr.ntruss.com/contest90-backend:posts-1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        # Write DB Configuration
        - name: MYSQL_HOST
          value: "user-mysql-service"
        - name: WRITE_DB_NAME
          value: "post_write_db"
        - name: WRITE_MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: post-write-db-password
        - name: WRITE_MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: post-write-db-user

        # Read DB Configuration
        - name: READ_DB_NAME
          value: "post_read_db"
        - name: READ_MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: post-read-db-password
        - name: READ_MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: post-read-db-user

        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"

        # JWT Configuration
        - name: jwtSecret
          valueFrom:
            secretKeyRef:
              name: jwtsecret-secret
              key: k

        # User Service Configuration
        - name: USER_SERVICE_URL
          value: "http://ing-default-samplealbing-bcdf6-110164755-b48ba1cb3cc4.kr.lb.naverncp.com"